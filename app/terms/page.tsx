import { CompanyInfoPage, companyPageMetadata } from "@/components/CompanyInfoPage";
export const metadata = companyPageMetadata("terms");
export default function TermsPage() { return <CompanyInfoPage pageKey="terms" />; }
